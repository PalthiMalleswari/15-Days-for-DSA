#### Use HashMap to count occurrences instead of loops.

#### How to Recognize

Look for:
  
  - Count
  
  - Frequency
  
  - Duplicates
  
   - Anagram / uniqueness
#### Sample Code
```
freq = {}
for x in array:
    freq[x] += 1
```
